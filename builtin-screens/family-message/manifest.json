{
  "id": "family-message",
  "name": "Family message",
  "description": "Displays a short message POSTed to the frame's /family-message endpoint. Unauthenticated on the LAN; rate-limited; off by default.",
  "config_schema": {
    "type": "object",
    "properties": {
      "background": { "type": "string", "default": "#0a1f2b" },
      "color": { "type": "string", "default": "#f5f5f5" }
    }
  }
}
