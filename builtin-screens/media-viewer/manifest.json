{
  "id": "media-viewer",
  "name": "Media viewer",
  "description": "Cycles through images/videos served by a directory on the LAN. Expects an index URL returning a JSON array of asset URLs.",
  "config_schema": {
    "type": "object",
    "required": ["index_url"],
    "properties": {
      "index_url": { "type": "string" },
      "interval_sec": { "type": "integer", "minimum": 3, "default": 15 },
      "fit": { "type": "string", "enum": ["cover", "contain"], "default": "contain" }
    }
  }
}
